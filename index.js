// Define num1 and num2 for multiplication
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // equal 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 10) + 1; // Ensures it's always greater than 0

// Define num3 and num4 for modulo calculation
const num3 = 24;
const num4 = 5;
const mod = num3 % num4; // Should equal 4

// Define max as the highest number in a set
const max = Math.max(1, 5, 10, 20); // Should return 20
